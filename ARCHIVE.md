# Archived build

This branch is a permanent, frozen snapshot of The Brief exactly as it existed before the `redesign-v2` work began (matches the `v1-design` tag / `main`'s state as of 2026-07-31).

It exists only so this version stays reachable at its own URL after `redesign-v2` eventually replaces it on `main`. Nothing on this branch should be edited or merged.
